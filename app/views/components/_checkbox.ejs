<div class="checkbox">
    <input
      type="checkbox"
      class="checkbox-element"
      id="<%= item.id %>"
      required="<%= item.required %>"
    />
    <label for="<%= item.id %>" class="checkbox-label">...</label> 
</div>