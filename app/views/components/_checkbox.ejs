<div class="checkbox">
    <input
      type="checkbox"
      class="checkbox-element"
      <%if (item.checked) { %>
        checked
      <%} %>
    />
    <label for="<%= item.id %>" class="checkbox-label">
      <span class="dots">...</span>
    </label> 
</div>